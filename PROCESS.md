# process log

## tools used
VS Code; git; UV

## One thing kept
Using `uv run` to execute python scripts because it manages environment dependencies automatically without manual setup.

## One thing rejected
One thing rejected: Running `check.py` from subdirectories, because it must be executed from the project root to inspect repo structures properly.

## AI usage
Search for relevant literature，
organize my thoughts, 
check the formatting of citations.